{{ form_start(form) }}
    {{ form_widget(form) }}
<button class="btn btn-success">{{ button_label|default("Valider") }}</button>
<a  class="btn btn-dark" href="{{ path('listes') }}">Retour</a>
{{ form_end(form) }}
